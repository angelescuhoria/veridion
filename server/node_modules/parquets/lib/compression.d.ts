/// <reference types="node" />
import { ParquetCompression } from './declare';
export interface ParquetCompressionKit {
    deflate: (value: Buffer) => Buffer;
    inflate: (value: Buffer, size: number) => Buffer;
}
export declare const PARQUET_COMPRESSION_METHODS: Record<ParquetCompression, ParquetCompressionKit>;
/**
 * Deflate a value using compression method `method`
 */
export declare function deflate(method: ParquetCompression, value: Buffer): Buffer;
/**
 * Inflate a value using compression method `method`
 */
export declare function inflate(method: ParquetCompression, value: Buffer, size: number): Buffer;
